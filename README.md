# ✈ Airline Booking System

This is a **menu-driven Python project** that simulates an airline ticket booking system.
It includes features like booking, cancellation, and PDF receipt generation, along with an **Admin Dashboard** using **Pandas** and **NumPy** for analytics.

## Features
- View Available Flights  
- Search Flights by City  
- Book Ticket (PDF Receipt)  
- View Bookings / Cancel Booking  
- Admin Dashboard (Summary + Analytics)

## Installation
```bash
pip install pandas
pip install numpy
pip install reportlab
